name: Setup
description: Install pnpm, Node, and the project's dependencies.

runs:
  using: composite

  steps:
    - name: Setup pnpm
      uses: pnpm/action-setup@v4

    - name: Setup Node
      uses: actions/setup-node@v7
      with:
        # Pinned so a runner image bump can't silently change the build's Node.
        node-version: 22
        cache: pnpm

    - name: Install dependencies
      run: pnpm install --frozen-lockfile
      shell: bash
