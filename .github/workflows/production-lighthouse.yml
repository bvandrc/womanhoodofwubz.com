name: Production Lighthouse

# On a push to main, polls for the deploy to land before running the
# Lighthouse audits. If no deploy is detected, the lighthouse job is
# skipped (wait-for-deploy still reports success, not cancelled).

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      target_url:
        description: URL to audit
        required: false
        default: https://womanhoodofwubz.neocities.org/

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  wait-for-deploy:
    name: Wait for deploy
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      deployed: ${{ steps.poll.outputs.deployed }}

    steps:
      - name: Poll for deploy
        id: poll
        uses: bvandrc/poll-for-deploy@v1
        with:
          url: https://womanhoodofwubz.neocities.org/

  lighthouse:
    name: Lighthouse
    needs: [wait-for-deploy]
    if: always() && (github.event_name == 'workflow_dispatch' || needs.wait-for-deploy.outputs.deployed == 'true')
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      PLAYWRIGHT_BASE_URL: ${{ github.event.inputs.target_url || 'https://womanhoodofwubz.neocities.org/' }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v7

      - name: Setup
        uses: ./.github/actions/setup

      - name: Setup Playwright
        uses: ./.github/actions/setup-playwright

      - name: Check target URL is reachable
        uses: jtalk/url-health-check-action@v5
        with:
          url: ${{ env.PLAYWRIGHT_BASE_URL }}
          max-attempts: 10
          retry-delay: 10s
          retry-all: true

      - name: Run Lighthouse audits
        run: pnpm test:lighthouse:production

      - name: Upload reports
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: lighthouse-reports
          path: playwright/results
          if-no-files-found: ignore
