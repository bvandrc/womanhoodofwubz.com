/**
 * Shared Sanity project coordinates.
 *
 * These are public identifiers — they appear in every request the browser makes
 * — so they live in source rather than in an env file. Shared with the studio,
 * which is a separate package: one copy stops the two from drifting apart.
 */

export const SANITY_PROJECT_ID = 'g2gixocz'
export const SANITY_DATASET = 'production'
